# SMOKE-TEST ONLY variant of config_finetune_full_cohort.yaml -- epochs
# capped low (3) purely to validate the new full_cohort pipeline mechanics
# (directory writes, config cross-linking, pickles, metrics files) quickly.
# NOT for real training -- the other 44 runs must use
# config_finetune_full_cohort.yaml (epochs=500, patience=50) unchanged.
model_params:
  embed_dim: 128
  num_heads: 4
  num_layers: 2
  num_classes: 5
  pooling_head: 4
  dropout: 0.3
  max_seq_length: 1500

max_channels: 4
context: -1
seed: 42
model: "SleepEventLSTMClassifier"
dataset: "mesa"
batch_size: 16
epochs: 3
lr: 0.0001
num_workers: 2
early_stopping_patience: 50

data_path: "/scratch/project_2019517/sleepfm-data/mesa/hdf5_full"
model_path: "/scratch/project_2019517/sleepfm-data/checkpoints/encoders/fromscratch_eegonly/SetTransformer/leave_one_out_128_patch_size_640"
split_path: "sleepfm/configs/dataset_split_fold5_v1.json"
labels_path: "/scratch/project_2019517/sleepfm-data/mesa/labels"
save_iter: 1000
eval_iter: 1000
log_interval: 10
accumulation_steps: 16
use_wandb: false

max_files: null
sampling_freq: 128

channel_like: ["EEG_ONLY"]
